module.exports = {
  plugins: [
    require('postcss-import'),
    require('autoprefixer')]
}

    // If icons/fonts break, uncomment this:
    // require('postcss-url')({ url: 'copy', assetsPath: 'app/assets/builds', useHash: true })
