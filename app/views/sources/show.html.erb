<h1><%= @source.title %></h1>

<p><strong>Author:</strong> <%= @source.author.presence || "" %></p>
<p><strong>Publisher:</strong> <%= @source.publisher.presence || "" %></p>
<p><strong>Year:</strong> <%= @source.year.presence || "" %></p>
<p><strong>Repository:</strong> <%= @source.repository.presence || "" %></p>
<p><strong>URL:</strong> <%= @source.url.present? ? link_to(@source.url, @source.url, target: "_blank", rel: "noopener") : "" %></p>
# <p><strong>Pages:</strong> <%#= @source.pages.presence || "" %></p>
# <p><strong>Common:</strong> <%= @source.common ? "" : "" %></p>
<p><strong>Details:</strong><br><%= simple_format(@source.details.to_s) %></p>

<p class="mt-3">
  <%= link_to "Edit", edit_source_path(@source), class: "btn btn-outline-secondary" %>
  <%= link_to "Back", sources_path, class: "btn btn-secondary ms-2" %>
</p>
