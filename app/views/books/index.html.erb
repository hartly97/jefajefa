<%= render 'introduction' %>

<div class="container-lg pb-5">
  <div class="row">
    <div class="col">
      <%# if user_signed_in? && (current_user.role == 'admin') %>
      <h5 class="text-color-blue">Click below to see the images and transcriptions</h5>
          <% @books.each do |book| %>
          <!-- <%#= link_to book.name, book_path(book) %> -->
            <%= link_to book.page_number, book_path(book) %>
            <!-- <%# if book.image.present? %>
            <%#= image_tag book.image.variant(resize_to_limit: [100, nil]) %>
            <%# end %>
          -->
          <% end %>
            <center>
              <div class="flickr_pagination mb-4">
                <%#= will_paginate @books, :container => false %>
              </div>
            </center>
    </div>
  </div>
</div>
