<%= form_with model: @record do |f| %>
  <div><%= f.label :name %><%= f.text_field :name %></div>
  <div><%= f.label :category_type %><%= f.text_field :category_type %></div>
  <div><%= f.label :description %><%= f.text_area :description %></div>
  <%= f.submit %>
<% end %>
