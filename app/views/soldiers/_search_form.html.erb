<%= form_with url: soldiers_path, method: :get, local: true, class: "row g-2 mb-3" do %>
  <div class="col-auto">
    <%= text_field_tag :q, params[:q], class: "form-control", placeholder: "Search name…" %>
  </div>
  <div class="col-auto">
    <%= number_field_tag :per_page, (params[:per_page] || 30), class: "form-control", min: 1, max: 200 %>
  </div>
  <div class="col-auto">
    <%= submit_tag "Search", class: "btn btn-primary" %>
  </div>
<% end %>
